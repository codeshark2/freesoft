# Server Configuration
PORT=8080
NODE_ENV=production

# Note: User API keys (Deepgram, OpenAI, ElevenLabs) are NOT stored as environment variables.
# They are provided by users at runtime and exist only in memory during sessions.
